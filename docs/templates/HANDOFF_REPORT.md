# 工作交接：<任务名称>

> 日期：  
> 分支：  
> HEAD：  
> 状态：已完成 / 部分完成 / 阻塞

## 结果

先用一段话说明用户目标是否达成，以及现在可以观察到的结果。

## 修改内容

- 

## 关键文件

| 文件 | 作用 |
| --- | --- |
|  |  |

## 验证证据

| 检查 | 结果 |
| --- | --- |
| `git diff --check` |  |
| `npm run typecheck` |  |
| `npm run test` |  |
| `npm run build` |  |
| Playwright / 浏览器 |  |

不要复制旧结果；只记录本次修改后实际执行过的检查。未执行写“未执行”并说明原因。

## Git / 部署

- commit：未提交 / `<sha> <message>`
- push：未推送 / `<remote>/<branch>`
- GitHub Actions：未触发 / `<run URL + conclusion>`
- 生产验证：未执行 / `<URL + result>`

## 已知限制与风险

- 无已知阻断项 / 

## 下一位代理的最短接手路径

1. 运行 `git status --short --branch`。
2. 阅读 `AGENTS.md` 与本交接。
3. 打开：
4. 先运行：
5. 下一步应做：

